# Bar Replay DEV.5

- Long/Short values and handles remain hover-driven.
- When replay reaches TP or SL, the visual position closes at the hit bar.
- Closed positions draw a dashed Entry-to-exit arrow, TradingView-style.
- Risk/reward background is clipped to the exit bar while the configured X handle is retained for editing.
- Outcome now exposes the exact TP/SL exit price for deterministic rendering.
- Backtest code remains separate from Bar Replay.
